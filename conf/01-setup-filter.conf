# Setup Filter

chain::INPUT::policy = accept
chain::INPUT::type = drop
chain::INPUT::log = output_packet

chain::OUTPUT::policy = accept
chain::OUTPUT::type = reject
chain::OUTPUT::log = input_packet

chain::FORWARD::policy = accept
chain::FORWARD::type = reject
chain::FORWARD::log = forward_packet

